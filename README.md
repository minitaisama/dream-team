# dream-team

Public knowledge/docs for the Dream Team setup:
- Coach (`pm-agent`)
- Lebron (`code-agent`)
- Curry (`qa-agent`)

This repo contains the current markdown/docs exported from the workspace for sharing and reference.
